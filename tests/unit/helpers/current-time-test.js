import { currentTime } from '../../../helpers/current-time';
import { module, test } from 'qunit';

module('Unit | Helper | current time');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = currentTime(42);
  assert.ok(result);
});
