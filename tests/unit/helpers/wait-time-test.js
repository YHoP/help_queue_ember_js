import { waitTime } from '../../../helpers/wait-time';
import { module, test } from 'qunit';

module('Unit | Helper | wait time');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = waitTime(42);
  assert.ok(result);
});
