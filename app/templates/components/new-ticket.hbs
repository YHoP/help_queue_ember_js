{{#if addNewTicket}}
<h1>New Ticket</h1>
   <div>
     <form>
       <div class="form-group">
         <label for="question">Question</label>
         {{input value=question id="question"}}
       </div>

       <button class="btn btn-success" {{action 'saveTicket' student}}>Save</button>
     </form>
   </div>
 {{else}}
    <button class="btn btn-info" {{action 'showTicketForm'}}>New Ticket</button>
{{/if}}
